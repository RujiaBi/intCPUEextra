% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_index.R
\name{plot_index}
\alias{plot_index}
\title{Plot standardized CPUE index and CV}
\usage{
plot_index(
  index_df,
  time_values = NULL,
  time_positions = NULL,
  x_text_angle = NULL,
  x_text_hjust = NULL,
  x_text_vjust = NULL
)
}
\arguments{
\item{index_df}{A data.frame returned by \code{\link[=get_index]{get_index()}} containing at least
\code{time}, \code{index}, and \code{cv}. If \code{areaid} is present, each area is drawn as a
separate colored series.}

\item{time_values}{Optional vector of labels for the x-axis. It may have the
same length as \code{nrow(index_df)} or the number of unique values in
\code{index_df$time}.}

\item{time_positions}{Optional numeric vector of x-axis positions. It may
have the same length as \code{nrow(index_df)} or the number of unique values in
\code{index_df$time}.}

\item{x_text_angle}{Optional numeric angle for x-axis text. If \code{NULL},
\code{plot_index()} uses \code{90} for year-month-like labels and \code{45} otherwise.}

\item{x_text_hjust}{Optional horizontal justification for x-axis text. If
\code{NULL}, a value matching \code{x_text_angle} is chosen automatically.}

\item{x_text_vjust}{Optional vertical justification for x-axis text. If
\code{NULL}, a value matching \code{x_text_angle} is chosen automatically.}
}
\value{
A ggplot object.
}
\description{
Creates a two-panel plot showing the standardized CPUE index and its
coefficient of variation (CV) over time.
}
