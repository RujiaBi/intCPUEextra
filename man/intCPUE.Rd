% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{intCPUE}
\alias{intCPUE}
\title{Fit a multi-area CPUE standardization model based on intCPUE}
\usage{
intCPUE(
  formula = NULL,
  data_utm,
  mesh,
  formula_catchability = NULL,
  formula_population = NULL,
  projection_data = NULL,
  area_col = NULL,
  flag_mesh = NULL,
  q_diffs_time = c("on", "off"),
  q_diffs_spatial = c("on", "off"),
  pop_spatiotemporal_type = c("rw", "ar1"),
  obs_sd_flag = c("shared", "flag"),
  obs_sd_area = c("area", "shared"),
  t_sd = NULL,
  control = list(eval.max = 1e+05, iter.max = 1e+05),
  ncores = NULL,
  ...,
  silent = FALSE,
  restart_max = 1L,
  newton_max = 2L,
  coord_max = 5L
)
}
\arguments{
\item{formula}{Legacy one-sided or two-sided model formula with optional
mgcv::s() smooth terms. If supplied, it is treated as
\code{formula_catchability}.}

\item{data_utm}{A data.frame with required columns.}

\item{mesh}{An \code{intCPUEmesh} or a bare fmesher mesh.}

\item{formula_catchability}{Optional one-sided or two-sided formula defining
smooth terms that affect catchability only.}

\item{formula_population}{Optional one-sided or two-sided formula defining
smooth terms that affect the latent population surface and therefore also
enter projection.}

\item{projection_data}{Optional projection-grid data for population smooth
covariates. Passed to \code{\link[=make_data]{make_data()}}. For static population covariates,
supply one row per extrapolation grid cell with columns \code{utm_x_scale},
\code{utm_y_scale}, and the covariate names used in \code{formula_population}. For
time-varying population covariates, additionally include \code{tid} and supply
one row per grid cell-time combination. If \code{formula_population} mixes
static and time-varying covariates, use the grid cell-time format and
repeat static covariate values across \code{tid}.}

\item{area_col}{Optional area column in \code{data_utm}. If supplied and it
identifies multiple areas, areas share vessel effects and catchability
smooths and flag-specific non-spatial effects, while fishery-level mean
differences and temporal/spatial population structure remain area-specific.
Use a named list of meshes with one entry per area when fitting multiple
areas.}

\item{flag_mesh}{Optional dedicated mesh for the shared flag-specific
spatial field (\code{flag_s}). When fitting multiple areas with
\code{q_diffs_spatial = "on"}, supply a single full-domain mesh here.}

\item{q_diffs_time}{"on" or "off". Controls whether flag-specific temporal
deviations are included. Implemented via dedicated TMB templates rather
than \code{map}.}

\item{q_diffs_spatial}{"on" or "off". Controls whether flag-specific spatial
deviations are included. Implemented via dedicated TMB templates rather
than \code{map}.}

\item{pop_spatiotemporal_type}{\code{"rw"} or \code{"ar1"}. Controls whether the
always-on spatiotemporal population field follows a random-walk or AR1
evolution over time.}

\item{obs_sd_flag}{\code{"shared"} or \code{"flag"}. Controls whether the
positive-catch lognormal observation SD is shared across flags or
estimated separately for each flag.}

\item{obs_sd_area}{\code{"area"} or \code{"shared"}. Controls whether the
positive-catch lognormal observation SD is estimated separately by area
or shared across areas. The default \code{"area"} preserves the historical
package behavior.}

\item{t_sd}{Optional positive scalar. Only used when \code{formula_population}
contains \code{f(tid)}. The default \code{NULL} estimates the random time-effect
standard deviation; a numeric value (for example \code{10}) fixes that standard
deviation at the supplied value.}

\item{control}{Control list passed to \code{\link[stats:nlminb]{stats::nlminb()}}.}

\item{ncores}{Optional integer. If provided, sets the number of OpenMP threads. Passed to \code{\link[TMB:openmp]{TMB::openmp()}}.}

\item{...}{Passed to \code{\link[=make_data]{make_data()}} (for example, \code{area_scale}).}

\item{silent}{Logical. Passed to \code{\link[TMB:MakeADFun]{TMB::MakeADFun()}}.}

\item{restart_max}{Non-negative integer. Maximum number of automatic
\code{nlminb()} restarts attempted from the current best parameter vector when
convergence or gradient checks remain unsatisfactory.}

\item{newton_max}{Non-negative integer. Maximum number of
\code{\link[TMB:newton]{TMB::newton()}} refinement attempts after \code{nlminb()}/restart stages.}

\item{coord_max}{Non-negative integer. Maximum number of coordinate-polish
iterations attempted after the restart/Newton stages. Set
\code{restart_max = 0}, \code{newton_max = 0}, and \code{coord_max = 0} to run only a
single \code{nlminb()} pass.}
}
\value{
An object of class \code{intCPUE} with elements \code{obj}, \code{opt}, \code{rep}, \code{prep}, etc.
}
\description{
Fits a multi-area extension of \code{intCPUE} for CPUE standardization. This
version is designed for settings where vessel effects and catchability
smooths are shared across areas, fishery-level mean differences and
flag-specific temporal deviations are shared across areas, and temporal
effects and latent population structure are estimated separately by area.
Flag-specific spatial deviations are modeled on a dedicated full-domain
mesh via \code{flag_mesh}, with their own anisotropy, range, and SD parameters.
}
\details{
The model is implemented using Template Model Builder (TMB). Spatial and
spatiotemporal random fields are represented using the SPDE approach for
computational efficiency. See the vignette for details:
https://github.com/RujiaBi/intCPUEextra/blob/main/vignettes/intCPUEextra-intro.Rmd
}
\author{
Rujia Bi \email{rbi@iattc.org}
}
